class Solution {
    public int numOfSubarrays(int[] arr) {
        int total = 0, currentOddSequence = 0, currentEvenSequence = 0, answer = 0;
        for (int i = 0; i < arr.length; i++) {

        }
        return total;
    }
}